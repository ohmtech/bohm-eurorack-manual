version: 2

submodules:
  exclude: all

sphinx:
  configuration: conf.py

python:
  install:
    - requirements: requirements.txt

formats:
  - htmlzip

build:
  os: ubuntu-20.04
  tools:
    python: "3.8"
